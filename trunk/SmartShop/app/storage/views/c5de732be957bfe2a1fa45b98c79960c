<?php foreach($arrayCate as $item): ?>
<?php if($item->catenewsParent==0): ?>
<li class="checkboxselect">
    <?php
    $checked = false;
    if (isset($catlistselect)) {
        $checked = in_array($item->id, $catlistselect);
    }
    ?>
    <?php echo Form::checkbox('catlist[]', $item->id,$checked); ?><?php echo $item->catenewsName; ?><br />
    <ul>
        <?php foreach($arrayCate as $item1): ?>
        <?php if($item->id==$item1->catenewsParent): ?>
        <?php
        $checked2 = false;
        if (isset($catlistselect)) {
            $checked2 = in_array($item1->id, $catlistselect);
        }
        ?>
        <li><?php echo Form::checkbox('catlist[]', $item1->id,$checked2); ?><?php echo $item1->catenewsName; ?><br /></li>
        <?php endif; ?>
        <?php endforeach; ?>
    </ul>
</li>
<?php endif; ?>
<?php endforeach; ?>
<script>
    jQuery('input:checkbox').uniform();</script>