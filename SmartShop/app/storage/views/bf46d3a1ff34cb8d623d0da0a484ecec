<?php
$i = 1;
if (Input::get('page') > 1) {
    $i = (Input::get('page') - 1) * 10 + 1;
}
$user_status = Lang::get('general.data_status1');
?>
<?php if(count($arrHistory)>0): ?>
<?php if(isset($objUser)): ?>
<input type="hidden" id="user_id" value="<?php echo $objUser->id; ?>" name="user_id"/>
<?php endif; ?>
<?php foreach($arrHistory as $item): ?>
<tr>
    <td><input name="checkboxidfile" type="checkbox" value="<?php echo $item->id; ?>"></td>

    <td><label value="page"></label><?php echo date('d/m/Y h:i:s', $item->time); ?></td>
    <td>
        <?php echo '<a href="javascript:void(0);" onclick="xxx1(this);" data-id="' . $item->id . '" data-email="' . $item->email . '" data-name="' . $item->firstname . ' ' . $item->lastname . '" data-content="' . $item->historyContent . '">' . str_limit($item->historyContent, 15, '...') . '</a>'; ?>
    </td>
    <td><label value="page">
            <?php
            if (array_key_exists($item->status, $user_status)) {
                echo $user_status[$item->status];
            }
            ?>
        </label>
    </td>
    <td>
        <?php if($item->status !=2): ?>
        <a href="javascript: void(0)" onclick="kichhoat('<?php echo $item->id; ?>', 2, jQuery('#user_id').val())" class="btn btn4 btn_trash"
           title="<?php echo Lang::get('button.trash'); ?>"></a>
        <?php endif; ?>
    </td>
</tr>
<?php endforeach; ?>
<script>
            jQuery('input:checkbox').uniform();</script>
<?php if($link!=''): ?>
<tr>
    <td colspan="7"><?php echo $link; ?></td>
</tr>
<?php endif; ?>
<?php else: ?>
<tr>
    <td colspan="7"><?php echo Lang::get('general.data_empty'); ?></td>
</tr>
<?php endif; ?>